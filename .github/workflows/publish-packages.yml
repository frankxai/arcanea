name: Publish Packages

on:
  push:
    branches: [main]
    paths:
      - 'packages/core/**'
      - 'packages/cli/**'
      - 'packages/arcanea-mcp/**'
      - 'packages/auth/**'
      - 'packages/overlay-*/**'
      - '.changeset/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build packages
        run: |
          pnpm --filter @arcanea/os run build
          pnpm --filter @arcanea/auth run build
          pnpm --filter "@arcanea/overlay-*" run build
          pnpm --filter @arcanea/mcp-server run build
          pnpm --filter @arcanea/cli run build

      - name: Run tests
        run: |
          node --test packages/core/tests/*.test.mjs packages/arcanea-mcp/tests/*.test.mjs packages/overlay-claude/tests/*.test.mjs packages/cli/tests/*.test.mjs

      - name: Create Release PR or Publish
        id: changesets
        uses: changesets/action@v1
        with:
          publish: pnpm changeset publish
          version: pnpm changeset version
          title: 'chore: version packages'
          commit: 'chore: version packages'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
